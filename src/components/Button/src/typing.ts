import type {
  ButtonProps as EleButtonProps,
  PopconfirmProps as ElePopconfirmProps,
} from 'element-plus'

export type ButtonProps = Partial<Writable<EleButtonProps>>

export type PopconfirmProps = Partial<Writable<ElePopconfirmProps>>
